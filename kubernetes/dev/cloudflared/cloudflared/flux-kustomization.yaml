# kubernetes/dev/cloudflared/cloudflared/flux-kustomization.yaml
apiVersion: kustomize.toolkit.fluxcd.io/v1beta1
kind: Kustomization
metadata:
  name: cloudflared-app
  namespace: flux-system
spec:
  interval: 1h  # Check every 1 hour
  path: "./kubernetes/dev/cloudflared/cloudflared"
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system
