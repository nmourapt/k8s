apiVersion: v1
kind: Service
metadata:
  name: image-automation-controller
  namespace: flux-system
spec:
  ports:
  - name: http
    port: 80
    targetPort: http
  selector:
    app: image-automation-controller
  type: ClusterIP